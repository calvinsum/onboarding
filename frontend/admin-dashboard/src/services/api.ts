import axios from "axios"; const API_BASE_URL = "http://localhost:3000/api"; const api = axios.create({ baseURL: API_BASE_URL, timeout: 10000, headers: { "Content-Type": "application/json" } }); export const dashboardAPI = { getDashboardStats: async () => { const response = await api.get("/admin/dashboard"); return response.data; }, getMerchants: async () => { const response = await api.get("/admin/merchants"); return response.data; }, getMerchantDetails: async (merchantId: string) => { const response = await api.get(`/admin/merchants/${merchantId}`); return response.data; }, startOnboarding: async (data: any) => { const response = await api.post("/onboarding/start", data); return response.data; } }; export default api;
