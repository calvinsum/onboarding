import React from "react"; import { Drawer, List, ListItem, ListItemIcon, ListItemText, Divider, Box, Typography } from "@mui/material"; import DashboardIcon from "@mui/icons-material/Dashboard"; import PeopleIcon from "@mui/icons-material/People"; import WarningIcon from "@mui/icons-material/Warning"; import AnalyticsIcon from "@mui/icons-material/Analytics"; import SettingsIcon from "@mui/icons-material/Settings"; interface SidebarProps { open: boolean; onToggle: () => void; } const drawerWidth = 280; const Sidebar: React.FC<SidebarProps> = ({ open }) => { const menuItems = [ { text: "Dashboard", icon: <DashboardIcon />, active: true }, { text: "Merchants", icon: <PeopleIcon />, active: false }, { text: "Escalations", icon: <WarningIcon />, active: false }, { text: "Analytics", icon: <AnalyticsIcon />, active: false }, { text: "Settings", icon: <SettingsIcon />, active: false }, ]; return ( <Drawer variant="persistent" anchor="left" open={open} sx={{ width: drawerWidth, flexShrink: 0, "& .MuiDrawer-paper": { width: drawerWidth, boxSizing: "border-box", backgroundColor: "#f8f9fa", borderRight: "1px solid rgba(0,0,0,0.08)" } }}> <Box sx={{ p: 2, textAlign: "center", backgroundColor: "primary.main", color: "white" }}> <Typography variant="h6" sx={{ fontWeight: 600 }}>Admin Panel</Typography> </Box> <Divider /> <List sx={{ pt: 2 }}> {menuItems.map((item) => ( <ListItem button key={item.text} sx={{ mx: 1, mb: 0.5, borderRadius: 2, backgroundColor: item.active ? "primary.light" : "transparent", "&:hover": { backgroundColor: item.active ? "primary.light" : "rgba(0,0,0,0.04)" } }}> <ListItemIcon sx={{ color: item.active ? "primary.main" : "text.secondary" }}> {item.icon} </ListItemIcon> <ListItemText primary={item.text} sx={{ "& .MuiTypography-root": { fontWeight: item.active ? 600 : 400, color: item.active ? "primary.main" : "text.primary" } }} /> </ListItem> ))} </List> </Drawer> ); }; export default Sidebar;
