import React from "react"; import { Card, CardContent, Typography, Box, Avatar } from "@mui/material"; interface StatsCardProps { title: string; value: number; icon: React.ReactNode; color: string; subtitle?: string; trend?: { value: number; isPositive: boolean; }; } const StatsCard: React.FC<StatsCardProps> = ({ title, value, icon, color, subtitle, trend }) => { return ( <Card sx={{ height: "100%", transition: "all 0.3s ease", "&:hover": { transform: "translateY(-4px)", boxShadow: "0 8px 30px rgba(0,0,0,0.12)" } }}> <CardContent sx={{ p: 3 }}> <Box sx={{ display: "flex", alignItems: "center", justifyContent: "space-between", mb: 2 }}> <Avatar sx={{ backgroundColor: color, width: 56, height: 56 }}> {icon} </Avatar> {trend && ( <Box sx={{ textAlign: "right" }}> <Typography variant="body2" sx={{ color: trend.isPositive ? "success.main" : "error.main", fontWeight: 600 }}> {trend.isPositive ? "+" : ""}{trend.value}% </Typography> <Typography variant="caption" color="text.secondary"> vs last week </Typography> </Box> )} </Box> <Typography variant="h4" sx={{ fontWeight: 700, mb: 1, color: "text.primary" }}> {value.toLocaleString()} </Typography> <Typography variant="h6" sx={{ color: "text.secondary", mb: 0.5 }}> {title} </Typography> {subtitle && ( <Typography variant="body2" sx={{ color: "text.secondary" }}> {subtitle} </Typography> )} </CardContent> </Card> ); }; export default StatsCard;
